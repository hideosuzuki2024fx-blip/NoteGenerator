name: "\ud86c\uff9f Publish Note"

on:
  workflow_dispatch:
    inputs:
      article_path:
        description: "Path to the note article"
        required: true
        type: string
      publish_mode:
        description: "Publish mode (test or production)"
        required: false
        default: "test"
        type: string

job:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: "\ud86c\uff9f Checkout"
        uses: actions/checkout@v3
      - name: "\ud86c\uff9f Debug inputs"
        run: |
          echo "👈 Article path: ${{ github.event.inputs.article_path }}"
          echo "👜 Publish mode: ${{ github.event.inputs.publish_mode }}"
      - name: "👌 Publish Note"
        run: |
          echo "Simulating publishing for article at: ${{ github.event.inputs.article_path }}"
          echo "Publish mode: ${{ github.event.inputs/publish_mode }}"
          # Actual publishing logic should go here
