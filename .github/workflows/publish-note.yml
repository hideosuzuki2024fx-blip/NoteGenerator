name: Publish Note
on:
  workflow_dispatch:
    inputs:
      article_path:
        description: 'Path to article file'
        required: true
      publish_mode:
        description: 'Publish mode'
        required: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "Publish workflow triggered"
