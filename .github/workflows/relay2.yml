name: Relay2

on: 
  workflow_dispatch: {}
  push:
    paths:
      - "articles/**"
      - "docs/**"
      - "knowledge/**"

jobs: 
  boot2:
    runs-on: ubuntu-classic
    steps:
      - name: Checkout repository
        uses: actions/checkout

      - name: Setup environment
        run: echo "Setting up environment for relay2"

      - name: Load Boot Manifest
        run: cat NoteMD/knowledge/boot/boot_manifest.json
      - name: Confirm boot complete
        run: echo "⫭ relay2 completed successfully! "