{
  "schema_version": "gpt-actions-catalog-v1",
  "source_of_truth": "Prompt-defined actions; OpenAPI not found in repo.",
  "operation_id_collision": true,
  "collision_note": "getRepositoryContents/createOrUpdateFile exist in both GitHub and Vercel providers.",
  "entries": [
    {
      "canonical_name": "gh_getRepoInfo",
      "bound_action": "getRepoInfo",
      "method": "GET",
      "path": "/repos/{owner}/{repo}",
      "purpose": "Fetch repository metadata.",
      "trigger_intent": "Need basic repo metadata or default branch.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "404 if repo/owner wrong → verify owner/repo",
        "401/403 if auth missing → use public endpoints or fix token"
      ],
      "fallback": "none"
    },
    {
      "canonical_name": "gh_getContents",
      "bound_action": "getRepositoryContents",
      "method": "GET",
      "path": "/repos/{owner}/{repo}/contents/{path}",
      "purpose": "Read files or list directory via GitHub contents API.",
      "trigger_intent": "Read file content or directory listing.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "404 if path missing → verify path",
        "403 rate limit → fallback to vx_getContents"
      ],
      "fallback": "vx_getContents"
    },
    {
      "canonical_name": "gh_putFile",
      "bound_action": "createOrUpdateFile",
      "method": "PUT",
      "path": "/repos/{owner}/{repo}/contents/{path}",
      "purpose": "Create/update files via GitHub contents API.",
      "trigger_intent": "Write binary or fallback text updates.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "Base64 encoding errors with Japanese text → prefer vx_putFile",
        "409 if SHA missing → fetch current SHA first"
      ],
      "fallback": "vx_putFile (text only)"
    },
    {
      "canonical_name": "gh_deleteFile",
      "bound_action": "deleteFile",
      "method": "DELETE",
      "path": "/repos/{owner}/{repo}/contents/{path}",
      "purpose": "Delete files in the repository.",
      "trigger_intent": "Remove a file.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "409 if SHA missing → fetch current SHA",
        "404 if path missing → verify path"
      ],
      "fallback": "none"
    },
    {
      "canonical_name": "gh_triggerWorkflow",
      "bound_action": "triggerWorkflow",
      "method": "POST",
      "path": "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches",
      "purpose": "Trigger a GitHub Actions workflow.",
      "trigger_intent": "Run CI or maintenance workflows.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "404 if workflow_id invalid → verify workflow file name/id",
        "403 if permissions missing → request proper token"
      ],
      "fallback": "none"
    },
    {
      "canonical_name": "vx_listRepoFiles",
      "bound_action": "listRepoFiles",
      "method": "GET",
      "path": "/api/github/list",
      "purpose": "List repository files through Vercel app.",
      "trigger_intent": "Enumerate repo via Vercel app when GitHub API fails.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "App errors → fallback to gh_getContents",
        "Unauthorized → verify app auth"
      ],
      "fallback": "gh_getContents"
    },
    {
      "canonical_name": "vx_getContents",
      "bound_action": "getRepositoryContents",
      "method": "GET",
      "path": "/api/github/getRepositoryContents",
      "purpose": "Read file content through Vercel app.",
      "trigger_intent": "Fallback read when GitHub contents API is blocked.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "App errors → fallback to gh_getContents",
        "Unauthorized → verify app auth"
      ],
      "fallback": "gh_getContents"
    },
    {
      "canonical_name": "vx_putFile",
      "bound_action": "createOrUpdateFile",
      "method": "POST",
      "path": "/api/github/createOrUpdateFile",
      "purpose": "Write text files through Vercel app (safer for Japanese text).",
      "trigger_intent": "Create/update text files (.md/.json/.yml/etc).",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "App errors → fallback to gh_putFile (text as last resort)",
        "Payload too large → split or use gh_putFile"
      ],
      "fallback": "gh_putFile"
    },
    {
      "canonical_name": "vx_saveArticle",
      "bound_action": "saveArticle",
      "method": "POST",
      "path": "/articles",
      "purpose": "Save a new article via Vercel app.",
      "trigger_intent": "Create article content without manual file writes.",
      "request_schema": "unknown (no OpenAPI in repo)",
      "response_shape": "unknown (no OpenAPI in repo)",
      "failure_modes": [
        "App errors → fallback to vx_putFile to NoteMD/articles/*",
        "Unauthorized → verify app auth"
      ],
      "fallback": "vx_putFile"
    }
  ]
}
