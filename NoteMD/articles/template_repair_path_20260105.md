# Template Repair with Path Discovery

This document defines an algorithm for discovering potential repair paths based on past conflicts, user selections, and template version relations.

It includes:

- Graph search of prior fixes
- Ranked path exploration based on repair counts
- User-preference bias applied to path selection
- Explicit state tracking for debuggability
