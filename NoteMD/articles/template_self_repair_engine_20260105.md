# Template Self-Repair Engine

This document specifies the script structure for a self-repairing module that follows diagnosis, debug repair, and revalidation logic.

The module automatically triggers prompt regeneration based on the current template state, and feedbacks results into the reconstruction loop.